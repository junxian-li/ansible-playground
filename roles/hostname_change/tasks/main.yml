---
# tasks file for hostname_change

- name: Network Getting Started First Playbook
  gather_facts: true
  hosts: all
  become: yes
  tasks:
    - name: Hello World
      debug:
        msg: "Hello World!!!"